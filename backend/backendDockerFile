FROM ubuntu:24.04
ENV DEBIAN_FRONTEND=noninteractive

RUN apt update && apt upgrade -y && apt install -y python3 
RUN echo 'ubuntu ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers
#RUN apt install sudo 
RUN apt update &&  apt install -y openssh-client 


RUN apt update 
RUN apt install -y git
# RUN apt install -y sudo
# ENTRYPOINT "tail -f /dev/null"  # keep container running
# CMD exec /bin/bash -c "trap : TERM INT; sleep infinity & wait"
# WORKDIR /app
# since we are mounting in docker compose the copy command is redundant. COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
# since we are mounting in docker compose the copy command is redundant. COPY . .
# WORKDIR /home/ubuntu

# USER ubuntu
